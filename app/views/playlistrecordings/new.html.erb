        <div class="col-md-6 col-md-offset-3">

          <% content_for :search do %>
            <%= search_form_for @q, url: new_playlist_playlistrecording_url do |f| %>
            <div class='input-group'>
              <%= f.search_field :title_or_description_or_city_or_country_or_tags_name_cont, class: "form-control", placeholder: "Find a recording to add" %>
              <span class="input-group-btn">
                 <button class="btn btn-default" type="submit">
                     <span class="glyphicon glyphicon-globe"></span>
                 </button>
              </span>
            <% end %>
          <% end %>
            </div>

<%=render 'track'%>
