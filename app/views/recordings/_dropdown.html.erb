                <div id="dropdown"%>
                <%=bootstrap_form_for recording_playlist_playlistrecordings_path(@recording, current_user.playlists.first), :html => { :id => "form" } do |f| %>
                <%= f.label :add_to_playlist %>:
                <%= f.collection_select :playlist_id, current_user.playlists, :id, :name,  :label => false  %>
                <%= f.hidden_field :recording_id, :value => @recording.id%>
                <%= form_tag params.merge(:show=>"recording")%>
                <%= f.submit value= "Add to Playlist" %>
                <%end%>
               </div%>
